{{#light-table table height="inherit" as |t|}}

  {{#unless isNested}}
    {{t.head onColumnClick=(action "onColumnClick")
             iconAscending="glyphicon glyphicon-menu-up" iconDescending="glyphicon glyphicon-menu-down"
             fixed=true}}
   {{/unless}}

  {{#t.body canSelect=false canExpand=true scrollBuffer=1000
            onRowClick=(action "onRowClick")
            rowComponent=(component "schema-table-row")
            onScrolledToBottom=(action "onScrolledToBottom") as |b|}}

    {{#unless isNested}}
      {{#b.expanded-row as |e|}}
        {{schema-table fields=(get e "enumeratedColumns") isNested=true height="auto"}}
      {{/b.expanded-row}}
    {{/unless}}

  {{/t.body}}

{{/light-table}}
